# TypeScript API Starter

Use `agents/typescript-api.agents.md` as the full source template. Copy it into the target repository root as `AGENTS.md`, then tune framework-specific details for Express, Fastify, or tRPC.
