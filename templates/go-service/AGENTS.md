# Go Service Starter

Use `agents/go-service.agents.md` as the full source template. Copy it into the target repository root as `AGENTS.md`, then adjust environment setup and package layout guidance if the service already has conventions.
