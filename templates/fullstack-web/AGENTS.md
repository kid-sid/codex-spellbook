# Fullstack Web Starter

Use `agents/fullstack-web.agents.md` as the full source template. Copy it into the target repository root as `AGENTS.md`, then preserve the project design system and test stack.
