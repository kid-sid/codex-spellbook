---
name: express-api
description: Scaffold a TypeScript Express API with strict typing and runtime validation.
category: scaffold
---

Scaffold a TypeScript Express API in `$TARGET_DIR`.

Create:

- strict `tsconfig.json`
- Express app with route modules
- Zod validation for request bodies and params
- Vitest setup
- Dockerfile and environment example

Constraints:

- no `any`
- structure code into routes, domain logic, and infrastructure
- include health endpoint and typed error handling
